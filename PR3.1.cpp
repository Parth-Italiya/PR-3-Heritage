/*1. WAP that defines a shape class with a constructor that gives valueto width and height. 
The define two sub-classes triangle and rectangle,
that calculate the area of the shape area (). In the main, define two variables
a triangle and a rectangle and then call the area() function in this two varibles.
*/

#include<iostream>
using namespace std;

class A{
	public:
		int id;
		int role;
		char name[];
};
class B{
	public:
		int salary;
		int experience;
};
class C{
	public:
		char name[];
		int salary;
		int role;
};
class D{
	public:
		char email[];
		int contact;
};
